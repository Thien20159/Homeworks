Primes_count = int(input("Nhập vào số lượng số nguyên tố cần hiển thị: "))
count  = 0
n = 1
while count < Primes_count:
    n += 1
    for i in range(2, n // 2 + 1):
        if n % i == 0:
            break
    else:
        print(f'Prime: {n} ')
        count += 1
